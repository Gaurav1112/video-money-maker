import React from 'react';

interface AnimatedOverlayProps {
  sceneType?: string;
}

/**
 * AnimatedOverlay — disabled for light educational theme.
 * Dark-theme effects (particles, scanlines, vignettes) removed.
 */
export const AnimatedOverlay: React.FC<AnimatedOverlayProps> = () => {
  return null;
};
