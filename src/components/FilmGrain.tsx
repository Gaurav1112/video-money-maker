import React from 'react';

/**
 * FilmGrain — disabled for light educational theme.
 * Film grain looks wrong on light backgrounds.
 */
export const FilmGrain: React.FC<{ opacity?: number }> = () => {
  return null;
};
