{
  "//": "Feature 008 — production-code-only type check. Extends the base",
  "//2": "config but excludes ALL test files. The base tsconfig.json keeps",
  "//3": "test files in scope for editor tooling; this variant is what the",
  "//4": "pre-commit hook and CI `tsc --noEmit` gate use, so the gate is",
  "//5": "clean today (the 18-failure / @types/jest gap is tracked in",
  "//6": "docs/test-burndown.md, not blocking commits).",
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "dist",
    "output",
    "src/api/**",
    "src/render/**",
    "src/services/quality-cache.ts",
    "src/services/queue/**",
    "src/audio/tts-engines/edge-tts-hinglish.ts",
    "src/pipeline/dual-track-orchestrator.ts",
    "src/pipeline/render-daily-short.patch.ts",
    "src/lib/metadata-generator.ts",
    "scripts/post-community.ts",
    "scripts/publish-telegram.ts",
    "scripts/queue/**",
    "scripts/migrate/queue-json-to-sqlite.ts",
    "**/__tests__/**",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/*.spec.ts"
  ]
}
